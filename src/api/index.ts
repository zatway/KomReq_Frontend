import * as AuthApi from "./auth";
import * as RequestsApi from "./requests";
import { setAuthToken } from "./http";

export { AuthApi, RequestsApi, setAuthToken };

export default {
    AuthApi,
    RequestsApi,
    setAuthToken,
};
